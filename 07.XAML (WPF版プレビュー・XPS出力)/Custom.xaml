    <Grid 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        mc:Ignorable="d"
        Background="#FFF9E79F"
              >
    <Grid.Resources>
      <!-- カスタムツールバースタイル -->
      <Style TargetType="{x:Type ToolBar}">
        <Setter Property="Background" Value="#FFF9E79F"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ToolBar}">
              <DockPanel LastChildFill="True">
                <ToolBarOverflowPanel x:Name="PART_ToolBarOverflowPanel" DockPanel.Dock="Right" Background="#FFF9E79F"/>
                <ScrollViewer x:Name="PART_ToolBarScrollViewer" CanContentScroll="True" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Focusable="False">
                  <ToolBarPanel x:Name="PART_ToolBarPanel" IsItemsHost="True" Orientation="Horizontal"/>
                </ScrollViewer>
              </DockPanel>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>


    <!-- ページ遷移VSCrollBar矢印をスクロールバーで行うためのスタイル -->
        <Style x:Key="FlatArrowButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="12"/>
            <!-- 他に必要なスタイルの設定があればここに追加 -->
        </Style>

        <!-- ボタンのスタイル -->
            <Style TargetType="Button">
                <Setter Property="Background" Value="#FFF9E79F"/>
                <Setter Property="BorderBrush" Value="#FFF7DC6B"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontFamily" Value="メイリオ"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="2" Direction="270" Opacity="0.84" Color="#333333"/>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#FFFAE6A0"/>
                        <Setter Property="BorderBrush" Value="#FFFAE17F"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect ShadowDepth="1" Direction="270" Opacity="0.1"/>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- テキストボックスのスタイル -->
            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Background" Value="#E0F7FA"/>
                <Setter Property="BorderBrush" Value="#B2EBF2"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="Padding" Value="8,6,6,4"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TextBox}">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                                <ScrollViewer Margin="0" x:Name="PART_ContentHost" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        <Style x:Key="ToggleStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="#FFF9E79F"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" BorderThickness="1" BorderBrush="Gray" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#FF2D2D30"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Grid.Resources>
       
            <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
              <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

      <StatusBar x:Name="statusStrip" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Height="22" FontSize="14" Margin="0,-4,0,0"  Background="#FFF9E79F">
        <StatusBarItem x:Name="helpStatus" Content="ヘルプステータス" Margin="8,0,0,0"/>
        <StatusBarItem HorizontalAlignment="Right">
          <StatusBarItem.Content>
            <TextBlock x:Name="timeStatus" Text="時刻" Margin="0,0,8,0"/>
          </StatusBarItem.Content>
        </StatusBarItem>
      </StatusBar>


      <Menu Grid.Row="1" Grid.ColumnSpan="1" x:Name="mnuTop" Background="#FFF9E79F">
            <MenuItem Header="ファイル(_F)" Name="mnuFile">
                <MenuItem Header="レポート定義ファイルの読込み(_R)..." Tag="レポート定義ファイルを開く" x:Name="mnuFileOpenDefine"/>
                <MenuItem Header="印刷データの読込み(_O)..." Tag="印刷データを開く" x:Name="mnuFileOpenData"/>
                <MenuItem Header="名前を付けて保存(_A)..." Tag="印刷データに名前を付けて保存する" x:Name="mnuFileSaveAs"/>
                <MenuItem Header="PDFファイルに保存(_D)..." Tag="ＰＤＦファイルに保存する" x:Name="mnuFileSavePDF"/>
                <Separator/>
                <MenuItem Header="印刷(_P)..." Tag="レポート定義/印刷データを印刷する" x:Name="mnuFilePrint"/>
                <!-- 最近使用したファイルの挿入位置 -->
                <MenuItem x:Name="mnuRecentFiles" Header="最近使ったファイル" Visibility="Collapsed"/>
                <Separator/>
                <MenuItem Header="終了(_X)" Tag="このウィンドウを閉じる" x:Name="mnuExit"/>
            </MenuItem>
            <MenuItem Header="表示(_V)" Tag="" x:Name="mnuView">
                <MenuItem Header="ツールバー(_T)" Tag="ツールバーの表示/非表示" x:Name="mnuViewToolBar" IsCheckable="True" IsChecked="True"/>
                <MenuItem Header="ステータスバー(_S)" Tag="ステータスバーの表示/非表示" x:Name="mnuViewStatusBar" IsCheckable="True" IsChecked="True"/>
                <Separator/>
                <MenuItem Header="ツールバー表示設定" Tag="ツールバーの表示変更" x:Name="menuViewToolbar">
                    <MenuItem Header="小アイコン" Tag="大きいアイコン / 小さいアイコン 切り替え" x:Name="menuVeiwToolBarSmallIcon" IsCheckable="True" IsChecked="False"/>
                    <MenuItem Header="テキスト表示" Tag="アイコンの下のテキストを表示 / 非表示" x:Name="menuVeiwToolBarDispText" IsCheckable="True" IsChecked="True"/>
                </MenuItem>
                <Separator/>
               <MenuItem x:Name="mnuXAML" Header="デフォルトはXAML版を起動" IsCheckable="True" IsChecked="True"/>
            </MenuItem>
            <MenuItem Header="ヘルプ(_H)" Tag="" x:Name="mnuHelp">
                <MenuItem Header="トピックの検索 (_O)..." Tag="" x:Name="mnuHelpOnline"/>
                <MenuItem Header="バージョン情報(_A)" Tag="" x:Name="mnuHelpAbout"/>
            </MenuItem>
        </Menu>


        <!-- Windows Forms の印刷プレビューコントロール。96dpiで丸められないのでプリンタ精度が出ます。上とどっちかを使うようにチェックボックスで切り替えられます。 -->
            <Grid x:Name="grdWinPrev" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
              <Grid.Resources>
                <SolidColorBrush x:Key="PreviewBackColor" Color="SKYBLUE"/>
              </Grid.Resources>
              <WindowsFormsHost Name="windowsFormsHost" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="4,0,0,0">
                <wf:PrintPreviewControl x:Name="previewCtl" />
                </WindowsFormsHost>
            </Grid>


      <DockPanel Grid.Row="3" Grid.ColumnSpan="3" x:Name="barTool" Background="#FFF9E79F">
        <ToolBarTray DockPanel.Dock="Bottom" Background="#FFF9E79F">
          <ToolBar Background="#FFF9E79F">
            <Button x:Name="tbOpen" ToolTip="開く" Background="#FFF9E79F">
              <StackPanel Orientation="Vertical" Background="#FFF9E79F">
                <Image x:Name="ImgOpen" Source="pack://application:,,,/Pao.Reports;component/Images/open.png"/>
                <TextBlock x:Name="txtOpen" Text="開く" FontSize="10" TextAlignment="Center"/>
              </StackPanel>
            </Button>
            <Button x:Name="tbSave" ToolTip="保存" Background="#FFF9E79F">
              <StackPanel Orientation="Vertical" Background="#FFF9E79F">
                <Image x:Name="ImgSave" Source="pack://application:,,,/Pao.Reports;component/Images/save.png"/>
                <TextBlock x:Name="txtSave" Text="保存" FontSize="10" TextAlignment="Center"/>
              </StackPanel>
            </Button>
            <Button x:Name="tbPrint" ToolTip="印刷" Background="#FFF9E79F">
              <StackPanel Orientation="Vertical" Background="#FFF9E79F">
                <Image x:Name="ImgPrint" Source="pack://application:,,,/Pao.Reports;component/Images/print.png"/>
                <TextBlock x:Name="txtPrint" Text="印刷" FontSize="10" TextAlignment="Center"/>
              </StackPanel>
            </Button>
            <Button x:Name="tbFirst" ToolTip="最初頁" Background="#FFF9E79F">
              <StackPanel Orientation="Vertical" Background="#FFF9E79F">
                <Image x:Name="ImgFirst" Source="pack://application:,,,/Pao.Reports;component/Images/first.png"/>
                <TextBlock x:Name="txtFirst" Text="最初頁" FontSize="10" TextAlignment="Center"/>
              </StackPanel>
            </Button>
            <Button x:Name="tbPrevious" ToolTip="前頁" Background="#FFF9E79F">
              <StackPanel Orientation="Vertical" Background="#FFF9E79F">
                <Image x:Name="ImgPrevious" Source="pack://application:,,,/Pao.Reports;component/Images/previous.png"/>
                <TextBlock x:Name="txtPrevious" Text="前頁" FontSize="10" TextAlignment="Center"/>
              </StackPanel>
            </Button>
            <Button x:Name="tbNext" ToolTip="次頁" Background="#FFF9E79F">
              <StackPanel Orientation="Vertical" Background="#FFF9E79F">
                <Image x:Name="ImgNext" Source="pack://application:,,,/Pao.Reports;component/Images/next.png"/>
                <TextBlock x:Name="txtNext" Text="次頁" FontSize="10" TextAlignment="Center"/>
              </StackPanel>
            </Button>
            <Button x:Name="tbLast" ToolTip="最終頁" Background="#FFF9E79F">
              <StackPanel Orientation="Vertical" Background="#FFF9E79F">
                <Image x:Name="ImgLast" Source="pack://application:,,,/Pao.Reports;component/Images/last.png"/>
                <TextBlock x:Name="txtLast" Text="最終頁" FontSize="10" TextAlignment="Center"/>
              </StackPanel>
            </Button>
          </ToolBar>

          <ToolBar Margin="10,0,0,0" Background="#FFF9E79F">
            <DockPanel Background="#FFF9E79F">
              <StackPanel Orientation="Horizontal" Background="#FFF9E79F">
                <TextBox x:Name="txtCurrent" Text="1" Width="50" TextAlignment="Right" FontSize="16" Height="36" Margin="10,0,0,0" Background="AliceBlue"/>
                <StackPanel DockPanel.Dock="Right" Orientation="Vertical">
                  <Button x:Name="UpButton" Content="▲" Width="20" Height="22" Style="{StaticResource FlatArrowButton}" Margin="0,1,0,0"/>
                  <Button x:Name="DownButton" Content="▼" Width="20" Height="22" Style="{StaticResource FlatArrowButton}" Margin="0,-7,0,0"/>
                </StackPanel>
                <Label Content=" /" VerticalAlignment="Center"/>
                <Label x:Name="lblTotalPage" Content="頁数" VerticalAlignment="Center" Margin="-4,0,0,0"/>
              </StackPanel>
            </DockPanel>
          </ToolBar>

          <ToolBar Background="#FFF9E79F">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10,0,0,0" Background="#FFF9E79F">
              <ToggleButton x:Name="btnAutoScaling" Style="{StaticResource ToggleStyle}" Content="自動スケーリング" Margin="5,0,0,0" IsChecked="False"  />
              <ToggleButton x:Name="btnZoom" Style="{StaticResource ToggleStyle}" Content="表示倍率指定" Margin="5,0,0,0" IsChecked="True"/>
              <ComboBox x:Name="cmbZoom" Width="60" Height="30" FontSize="16" Margin="5,0,0,0">
                <ComboBox.ItemTemplate>
                  <DataTemplate>
                    <TextBlock Text="{Binding}" HorizontalAlignment="Right"/>
                  </DataTemplate>
                </ComboBox.ItemTemplate>
                <ComboBox.ItemContainerStyle>
                  <!-- ComboBoxItem のスタイルを定義 -->
                  <Style TargetType="ComboBoxItem">
                    <Setter Property="Height" Value="30"/>
                    <Setter Property="FontSize" Value="16"/>
                  </Style>
                </ComboBox.ItemContainerStyle>
                <ComboBoxItem Content="50"/>
                <ComboBoxItem Content="75"/>
                <ComboBoxItem Content="80"/>
                <ComboBoxItem Content="85"/>
                <ComboBoxItem Content="90"/>
                <ComboBoxItem Content="95"/>
                <ComboBoxItem Content="100" IsSelected="True"/>
                <ComboBoxItem Content="105"/>
                <ComboBoxItem Content="110"/>
                <ComboBoxItem Content="115"/>
                <ComboBoxItem Content="125"/>
                <ComboBoxItem Content="150"/>
                <ComboBoxItem Content="200"/>
              </ComboBox>
              <Label x:Name="lblPer" Content="%" VerticalAlignment="Center" Margin="-2"/>
            </StackPanel>
          </ToolBar>
        </ToolBarTray>
      </DockPanel>

    </Grid>